[1mdiff --git a/titan_hud.py b/titan_hud.py[m
[1mindex c9bf633..39b5b6c 100644[m
[1m--- a/titan_hud.py[m
[1m+++ b/titan_hud.py[m
[36m@@ -7,12 +7,19 @@[m [mimport pyautogui[m
 import numpy as np[m
 import time[m
 import math[m
[32m+[m[32mimport pyaudio[m
[32m+[m[32mimport wave[m
[32m+[m[32mimport winsound[m
[32m+[m[32mimport subprocess[m
[32m+[m[32mimport psutil[m
 [m
 from PySide6.QtWidgets import ([m
     QApplication, QLabel, QWidget, QVBoxLayout, QHBoxLayout, QPushButton[m
 )[m
 from PySide6.QtCore import Qt, QThread, Signal, QPoint[m
 from PySide6.QtGui import QImage, QPixmap[m
[32m+[m[32mfrom openwakeword.model import Model as OWWModel[m
[32m+[m[32mfrom faster_whisper import WhisperModel[m
 [m
 [m
 # ================= SETTINGS ================= #[m
[36m@@ -397,7 +404,123 @@[m [mclass GestureThread(QThread):[m
         self.running = False[m
         [m
       [m
[32m+[m[32m# ================= VOICE THREAD ================= #[m
 [m
[32m+[m[32mclass VoiceThread(QThread):[m
[32m+[m[32m    voice_status = Signal(str)[m
[32m+[m
[32m+[m[32m    def __init__(self):[m
[32m+[m[32m        super().__init__()[m
[32m+[m[32m        self.running = True[m
[32m+[m
[32m+[m[32m    def handle_command(self, text):[m
[32m+[m[32m        text = text.lower()[m
[32m+[m
[32m+[m[32m        if "battery" in text:[m
[32m+[m[32m            battery = psutil.sensors_battery()[m
[32m+[m[32m            if battery:[m
[32m+[m[32m                msg = f"Battery is at {int(battery.percent)}%"[m
[32m+[m[32m            else:[m
[32m+[m[32m                msg = "Battery info not available"[m
[32m+[m
[32m+[m[32m        elif "volume up" in text:[m
[32m+[m[32m            for _ in range(5):[m
[32m+[m[32m                pyautogui.press("volumeup")[m
[32m+[m[32m            msg = "Volume increased"[m
[32m+[m
[32m+[m[32m        elif "volume down" in text:[m
[32m+[m[32m            for _ in range(5):[m
[32m+[m[32m                pyautogui.press("volumedown")[m
[32m+[m[32m            msg = "Volume decreased"[m
[32m+[m
[32m+[m[32m        elif "mute" in text:[m
[32m+[m[32m            pyautogui.press("volumemute")[m
[32m+[m[32m            msg = "Muted"[m
[32m+[m
[32m+[m[32m        elif "chrome" in text:[m
[32m+[m[32m            subprocess.Popen(["start", "chrome"], shell=True)[m
[32m+[m[32m            msg = "Opening Chrome"[m
[32m+[m
[32m+[m[32m        elif "notepad" in text:[m
[32m+[m[32m            subprocess.Popen(["notepad.exe"])[m
[32m+[m[32m            msg = "Opening Notepad"[m
[32m+[m
[32m+[m[32m        elif "scroll up" in text:[m
[32m+[m[32m            pyautogui.scroll(300)[m
[32m+[m[32m            msg = "Scrolled up"[m
[32m+[m
[32m+[m[32m        elif "scroll down" in text:[m
[32m+[m[32m            pyautogui.scroll(-300)[m
[32m+[m[32m            msg = "Scrolled down"[m
[32m+[m
[32m+[m[32m        else:[m
[32m+[m[32m            msg = f"No matching command for: \"{text}\""[m
[32m+[m
[32m+[m[32m        self.voice_status.emit(f"Heard: \"{text}\" → {msg}")[m
[32m+[m
[32m+[m[32m    def run(self):[m
[32m+[m[32m        owwModel = OWWModel(wakeword_models=["hey_jarvis"])[m
[32m+[m[32m        whisper_model = WhisperModel("small.en", device="cpu", compute_type="int8")[m
[32m+[m
[32m+[m[32m        FORMAT = pyaudio.paInt16[m
[32m+[m[32m        CHANNELS = 1[m
[32m+[m[32m        RATE = 16000[m
[32m+[m[32m        CHUNK = 1280[m
[32m+[m[32m        RECORD_SECONDS = 4[m
[32m+[m[32m        COOLDOWN_SECONDS = 2[m
[32m+[m
[32m+[m[32m        audio = pyaudio.PyAudio()[m
[32m+[m[32m        stream = audio.open(format=FORMAT, channels=CHANNELS, rate=RATE,[m
[32m+[m[32m                             input=True, frames_per_buffer=CHUNK)[m
[32m+[m
[32m+[m[32m        self.voice_status.emit("Listening for 'Hey Jarvis'...")[m
[32m+[m
[32m+[m[32m        last_trigger_time = 0[m
[32m+[m[32m        armed = True[m
[32m+[m
[32m+[m[32m        while self.running:[m
[32m+[m[32m            audio_chunk = np.frombuffer(stream.read(CHUNK, exception_on_overflow=False), dtype=np.int16)[m
[32m+[m[32m            prediction = owwModel.predict(audio_chunk)[m
[32m+[m
[32m+[m[32m            now = time.time()[m
[32m+[m
[32m+[m[32m            for wakeword, score in prediction.items():[m
[32m+[m[32m                if score > 0.5 and armed and (now - last_trigger_time) > COOLDOWN_SECONDS:[m
[32m+[m[32m                    armed = False[m
[32m+[m
[32m+[m[32m                    winsound.Beep(1000, 150)[m
[32m+[m[32m                    self.voice_status.emit("Listening for command...")[m
[32m+[m
[32m+[m[32m                    frames = [][m
[32m+[m[32m                    num_chunks = int(RATE / CHUNK * RECORD_SECONDS)[m
[32m+[m[32m                    for _ in range(num_chunks):[m
[32m+[m[32m                        data = stream.read(CHUNK, exception_on_overflow=False)[m
[32m+[m[32m                        frames.append(data)[m
[32m+[m
[32m+[m[32m                    wf = wave.open("command.wav", "wb")[m
[32m+[m[32m                    wf.setnchannels(CHANNELS)[m
[32m+[m[32m                    wf.setsampwidth(audio.get_sample_size(FORMAT))[m
[32m+[m[32m                    wf.setframerate(RATE)[m
[32m+[m[32m                    wf.writeframes(b"".join(frames))[m
[32m+[m[32m                    wf.close()[m
[32m+[m
[32m+[m[32m                    segments, _ = whisper_model.transcribe("command.wav")[m
[32m+[m[32m                    text = " ".join([seg.text for seg in segments]).strip()[m
[32m+[m
[32m+[m[32m                    self.handle_command(text)[m
[32m+[m[32m                    winsound.Beep(1500, 100)[m
[32m+[m
[32m+[m[32m                    last_trigger_time = time.time()[m
[32m+[m
[32m+[m[32m                elif score < 0.2:[m
[32m+[m[32m                    armed = True[m
[32m+[m
[32m+[m[32m        stream.stop_stream()[m
[32m+[m[32m        stream.close()[m
[32m+[m[32m        audio.terminate()[m
[32m+[m
[32m+[m[32m    def stop(self):[m
[32m+[m[32m        self.running = False[m
 [m
 # ================= HUD WINDOW ================= #[m
 [m
[36m@@ -438,6 +561,9 @@[m [mclass TitanHUD(QWidget):[m
         self.status_label = QLabel("Status: Starting...")[m
         self.fingers_label = QLabel("Fingers: -")[m
         self.pinch_label = QLabel("Pinch Ratio: -")[m
[32m+[m[32m        self.voice_label = QLabel("Voice: Starting...")[m
[32m+[m[32m        self.voice_label.setStyleSheet("color: #ffaa00; font-size: 14px;")[m
[32m+[m[32m        self.voice_label.setWordWrap(True)[m
 [m
         for lbl in (self.status_label, self.fingers_label, self.pinch_label):[m
             lbl.setStyleSheet("color: white; font-size: 16px;")[m
[36m@@ -448,6 +574,7 @@[m [mclass TitanHUD(QWidget):[m
         outer_layout.addWidget(self.status_label)[m
         outer_layout.addWidget(self.fingers_label)[m
         outer_layout.addWidget(self.pinch_label)[m
[32m+[m[32m        outer_layout.addWidget(self.voice_label)[m
 [m
         self.setLayout(outer_layout)[m
         self.setStyleSheet("""[m
[36m@@ -463,6 +590,9 @@[m [mclass TitanHUD(QWidget):[m
         self.gesture_thread.status_updated.connect(self.update_status)[m
         self.gesture_thread.mode_changed.connect(self.update_mode_label)[m
         self.gesture_thread.start()[m
[32m+[m[32m        self.voice_thread = VoiceThread()[m
[32m+[m[32m        self.voice_thread.voice_status.connect(self.update_voice_status)[m
[32m+[m[32m        self.voice_thread.start()[m
 [m
     def update_frame(self, frame):[m
         rgb = cv2.cvtColor(frame, cv2.COLOR_BGR2RGB)[m
[36m@@ -473,6 +603,9 @@[m [mclass TitanHUD(QWidget):[m
         )[m
         self.camera_label.setPixmap(pixmap)[m
 [m
[32m+[m[32m    def update_voice_status(self, text):[m
[32m+[m[32m        self.voice_label.setText(f"Voice: {text}")[m
[32m+[m
     def update_status(self, status, fingers, pinch_ratio):[m
         self.status_label.setText(f"Status: {status}")[m
         self.fingers_label.setText(f"Fingers: {fingers}")[m
[36m@@ -503,9 +636,9 @@[m [mclass TitanHUD(QWidget):[m
 [m
     def closeEvent(self, event):[m
         self.gesture_thread.stop()[m
[31m-        if not self.gesture_thread.wait(3000):   # wait max 3 seconds[m
[31m-            self.gesture_thread.terminate()      # force-kill if still stuck[m
[31m-            self.gesture_thread.wait()[m
[32m+[m[32m        self.gesture_thread.wait()[m
[32m+[m[32m        self.voice_thread.stop()[m
[32m+[m[32m        self.voice_thread.wait(2000)[m
         event.accept()[m
 [m
 app = QApplication(sys.argv)[m
